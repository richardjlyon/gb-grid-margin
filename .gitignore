# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/

# OS
.DS_Store

# Private working notes & local agent context — never commit
_private/
CLAUDE.md

# Local scratch
/tmp/
*.log
.playwright-mcp/
.memsearch/
.superpowers/

# Node (site tooling, if any later)
node_modules/

# Dev-only live-layer validation harness — never shipped to prod (fault-injection page)
site/validate.html
